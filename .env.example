# Copy to .env.local if you want to override anything during local dev.
# NOTE: Do NOT put API keys here — they are stored in Windows Credential
# Manager via the keystore_set Tauri command (wired up in M4).

# Force vite dev host (useful when running `tauri dev` from another machine)
# TAURI_DEV_HOST=0.0.0.0
